                                                                                                                                                                                                                                                                               /var/ossec/etc/rules/local_rules.xml                                                                                                                                                                                                                                                                                              


<!-- Local rules -->

<!-- Modify it at your will. -->
<!-- Copyright (C) 2015, Wazuh Inc. -->

<!-- Example -->
<group name="local,syslog,sshd,">

  <!--
  Dec 10 01:02:02 host sshd[1234]: Failed none for root from 1.1.1.1 port 1066 ssh2
  -->
  <rule id="100001" level="5">
    <if_sid>5716</if_sid>
    <srcip>1.1.1.1</srcip>
    <description>sshd: authentication failed from IP 1.1.1.1.</description>
    <group>authentication_failed,pci_dss_10.2.4,pci_dss_10.2.5,</group>
  </rule>

</group>

<group name="pfsense,syslog">
  <rule id="100023" level="5">
    <decoded_as>pfsense</decoded_as>
    <field name="firewallaction">pass</field>
    <description>Traffic from $(srcip) to $(dstip) passed.</description>
  </rule>
</group>

<group name="pfsense,ssh,network">

 <!-- Allow ssh ingress connections-->

  <rule id="100110" level="5">
   <decoded_as>pfsense-filterlog</decoded_as>
   <field name="dstport">22</field>
   <field name="firewallaction">pass</field>
   <description>Inbound SSH connection from $(srcip) to $(dstip) allowed through pfSense</description>
  </rule>

  <!-- Block ssh ingress connections-->

  <rule id="100111" level="10">
   <decoded_as>pfsense-filterlog</decoded_as>
   <field name="dstport">22</field>
   <field name="firewallaction">block</field>
   <description>Inbound SSH connection from $(srcip) to $(dstip) Blocked by pfSense</description>
  </rule>

 <!-- Inbound ssh  connections Alert -->

  <rule id="100112" level="10">
   <decoded_as>pfsense-filterlog</decoded_as>
   <field name="dstport">22</field>
   <field name="direction">in</field>
   <description>Inbound SSH attempt from $(srcip) via pfSense/WAN</description>
  </rule>

 <!-- Inbound ssh Brute Force Alert -->
  <rule id="100113" level="12" frequency="5" timeframe="60">
   <if_matched_sid>100112</if_matched_sid>
   <same_source_ip/>
   <description>Multiple SSH blocks $(srcip) on WAN detected (possible brute force)</description>
  </rule>

 <!-- Inbound ssh Brute Force Alert -->

  <rule id="100114" level="12" frequency="5" timeframe="60">
   <if_matched_sid>100111</if_matched_sid>
   <same_source_ip/>
   <description>Multiple SSH attempts from $(srcip) on WAN (possible brute force)</description>
  </rule>

</group>

<group name="pfsense,icmp,network">

  <rule id="100122" level="12" frequency="5" timeframe="30">
   <decoded_as>pfsense-filterlog</decoded_as>
   <field name="protocol">icmp</field>
   <field name="direction">in</field>
   <same_source_ip/>
   <description>Inbound ICMP scan detected (possible reconnaissance)</description>
  </rule>

</group>

